interface MyBrigadeMilitaryPersonnel {
  id: number;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  placeOfBirth: string;
  rank: string | null;
  status: string | null;
}

export type { MyBrigadeMilitaryPersonnel };
